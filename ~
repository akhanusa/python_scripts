#!/usr/bin

import re

devlist = dict()

x = raw_input("Enter site name:")

y = x.strip()

devicesFile = open("testfile", "r")

for line in devicesFile:
   print(line.split('~')[1])

'''
for line in devicesFile:
   z = re.search(y,line.split("~")[5].strip(),re.IGNORECASE)
   if z:
     devlist[line.split("~")[1].strip()] = line.split("~")[0].strip()

for i in devlist:
   print i

a = raw_input("Enter device name:")

b = a.strip()

for i in devlist:
   c = re.search(b,str(i),re.IGNORECASE)
   if c:
      print i + ':' + ' ' + devlist[i]
'''

devicesFile.close()
